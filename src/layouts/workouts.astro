---
const {frontmatter} = Astro.props;
---
<html>
  <!-- ... -->
  <h1>{frontmatter.title}</h1>
  <p>Training type: {frontmatter.type}</p>
  <slot /> <!-- Markdown content is injected here -->
   <!-- ... -->
</html>